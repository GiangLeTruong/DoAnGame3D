using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(fileName = "newClass",menuName = "ScriptableObject/LevelSetup",order =1)] 
public class newClass : ScriptableObject
{

    public int Level;
    public int Character;
    public int Enemy;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }






}
